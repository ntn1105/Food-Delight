extends ../../layout/defaut.pug

block main  
    h1(class="mb-4") Thêm nhóm phân quyền

    form(
        action=`${prefixAdmin}/roles/create`
        method="POSt"
    )
        div(class="form-group")
            label(for="title") Tiêu đề
            input(
                type="text"
                class="form-control"
                id="title"
                name="title"
                required
            )
        div(class="form-group")
            label(for="title") Mô tả
            input(
                type="text"
                class="form-control"
                id="title"
                name="title"
                required
            )
        